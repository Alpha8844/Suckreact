{
  "version": 2,
  "routes": [
    {
      "src": "/g/(.*)",
      "dest": "/g/$1"
    }
  ],
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist" }
    }
  ]
} 